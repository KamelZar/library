spring:
  datasource:
    url: jdbc:h2:mem:librarydb-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  admin:
    username: admin
    password: test-password
  security:
    max-login-attempts: 3
    blacklist-duration-minutes: 15
